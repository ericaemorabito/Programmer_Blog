 <!-- Home Page -->
    <div class="container-fluid text-center">
      <div class="row">
        <div class="col mt-4 mb-4"> 
          <h1>Home</h1>
        </div>
      </div>

  <div class="row justify-content-around">
    <!-- Each Review Added -->
    {{#each posts as |post| }}
    {{> posts}}
    {{/each}}
  </div>
  
</div>